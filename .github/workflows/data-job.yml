name: Phase 4 Data Job

on:
  workflow_dispatch:
    inputs:
      request_json:
        description: Validated Phase 3 Job Request v0.1 JSON
        required: true
        type: string
      request_note:
        description: Optional human note; non-authoritative
        required: false
        type: string

permissions:
  contents: read

jobs:
  preflight:
    name: Validate request
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      request_key: ${{ steps.validate.outputs.request_key }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Validate Phase 4 request
        id: validate
        env:
          HDH_REQUEST_JSON: ${{ inputs.request_json }}
        run: node dist/src/actions/request-cli.js

  execute-p4-3:
    name: Execute QUICK / RESEARCH
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 180
    concurrency:
      group: hdh-phase4-${{ needs.preflight.outputs.request_key }}
      cancel-in-progress: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Revalidate request in execution workspace
        env:
          HDH_REQUEST_JSON: ${{ inputs.request_json }}
        run: node dist/src/actions/request-cli.js

      - name: Execute accepted Core path
        run: node dist/src/actions/execute-cli.js

      - name: P4.3 transport boundary
        run: echo "Execution output remains runner-local until P4.5 Artifact transport is accepted."
